# 2026-09-25

## What happened

- Private proposal project (in `projects/_private/`): built a downloadable guideline/drug-knowledge corpus to back the
  QA system (317 files, ~434 MB, kept out of git) and wrote a source-inventory + ingestion plan.
- Classified each source by how it is published: web page → PDFs, web + PDF, structured XML, or gated/dynamic.
- Ran integrity and text-layer checks on every PDF (all born-digital, no OCR needed).

## What we learned (shareable)

- Verify before you trust: three of my own first-pass results were wrong (a fabricated link, an HTML error page
  saved as a "PDF", and ancillary files mistaken for guidelines). Cheap checks (file header/end marker, page count,
  reading the licence text) caught all of them.
- Government sites move; old document links rot. Snapshotting + hashing sources and tracking versions is a
  requirement for any guideline-grounded system, not an extra.
- For clinical knowledge bases the licence, not the download, is the gating factor.
- A useful pipeline shape for document-to-knowledge-graph ingestion: per-format parsing → clause-level segmentation →
  LLM extraction into a strict schema → quote-verified provenance → expert review → versioned graph.

## Open / next

- [ ] Manual downloads for sources that block scripts; licence enquiries (details in the private README)
- [ ] Decide ingestion tooling; run a small 3-document pilot

## Update (later the same day)

- Second download round: found more legitimately downloadable guidelines (open-access papers, kidney and stroke/obesity
  guidelines, more HSE documents) and separated what needs a licence, registration or a manual step.
- Produced a single Word document for team discussion (kept in the private folder, not in this repo).
- Learned: some sources are more open than assumed (e.g. medical-code standards are free in Ireland with registration;
  some journals publish open-access PDFs), while others that look free are not reusable in a product.
- Learned (again): re-check derived claims against the data before writing them down; a "no OCR needed" statement had to
  be narrowed after a wider check.
